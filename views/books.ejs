<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <header class="mb-8">
            <h1 class="text-4xl font-bold text-gray-800 mb-4">📚 StoryStack 📇</h1>
        </header>


        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            <% books.forEach(book => { %>
                <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-all transform hover:-translate-y-1 p-6" data-book-id="<%= book._id %>">
                    <h2 class="text-xl font-semibold text-gray-800 mb-2">
                        <a href="/books/<%= book._id %>" class="hover:text-blue-600 transition-colors">
                            <%= book.name %>
                        </a>
                    </h2>
                    <div class="flex justify-between items-center pt-4 border-t">

                        <a href="/books/<%= book._id %>/edit"
                            class="inline-flex items-center px-3 py-2 bg-blue-500 text-white text-sm rounded-lg hover:bg-blue-600 transition">
                             <i class="fas fa-edit"></i> Update book
                         </a>

                    </div>
                    <div class="flex justify-between items-center pt-4 border-t">
                        <a href="/books/<%= book._id %>" 
                            class="inline-flex items-center px-3 py-2 bg-blue-500 text-white text-sm rounded-lg hover:bg-blue-600 transition">
                             <i class="fas fa-info-circle mr-2"></i> View Details
                         </a>
     
                    </div>

                </div>
            <% }) %>
        </div>

        <div class="mt-8">
            <a href="/books/new" class="w-full sm:w-auto bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors text-center">
                <i class="fas fa-plus mr-2"></i> Add New Book
            </a>
        </div>
    </div>
</body>
</html>